#include "main.h"

/**
 * _abs - Prints absolute number of a number
 *
 *@n: Parameter given to abs
 *
 * Return: absolute number
 */
int _abs(int n)
{
	if (!(n == 0))
		return (n);
	else
		return (0);
	_putchar('\n');
}
