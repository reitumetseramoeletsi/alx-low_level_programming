#include "main.h"

/**
 *_isupper - checks for uppercase letter
 *
 * @c: given variable
 *
 * Return: Always 0.
 */
int _is upper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
